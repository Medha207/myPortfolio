import puppeteer from 'puppeteer'; (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173'); await new Promise(r => setTimeout(r, 4000)); await page.screenshot({path: 'screenshot_5173.png'}); const title = await page.title(); console.log('TITLE:', title); const rootHTML = await page.\('#root', el => el.innerHTML); console.log('ROOT LENGTH:', rootHTML.length); await browser.close(); })();
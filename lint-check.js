const { execSync } = require('child_process'); try { execSync('npx eslint . --format json > lint_json.json', { stdio: 'ignore' }); } catch(e) {} const fs = require('fs'); const data = JSON.parse(fs.readFileSync('lint_json.json', 'utf8')); data.forEach(f => { if(f.errorCount) { console.log('\n--- ' + f.filePath + ' ---'); f.messages.forEach(m => console.log('Line ' + m.line + ': ' + m.message)); } });